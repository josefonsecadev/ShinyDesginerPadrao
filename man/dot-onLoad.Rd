% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navbar.R
\name{.onLoad}
\alias{.onLoad}
\title{Cria um layout padronizado para dashboards Shiny}
\usage{
.onLoad(libname, pkgname)
}
\arguments{
\item{title}{Texto exibido como título do dashboard.}

\item{menu_items}{Lista com as telas do dashboard. Cada item pode ser
um objeto de UI do Shiny ou uma lista com os elementos \code{label} e \code{ui}.}

\item{theme}{Tema do bslib para personalizar cores e estilo.}

\item{include_css}{Lógico indicando se deve incluir o estilo padrão do pacote.}

\item{...}{Argumentos adicionais enviados para \code{\link[bslib:page_navbar]{bslib::page_navbar()}}.}
}
\value{
Um objeto de interface do Shiny com o layout padronizado.
}
\description{
Esta função recebe um título e uma lista de telas para montar um
dashboard com menu navegável, visual reutilizável e conteúdo configurável.
}
\examples{
\dontrun{
menu_items <- list(
  "visao_geral" = list(
    label = "Visão Geral",
    ui = shiny::fluidPage(
      shiny::h2("Visão Geral"),
      shiny::p("Conteúdo da primeira tela")
    )
  ),
  "vendas" = list(
    label = "Vendas",
    ui = shiny::fluidPage(
      shiny::h2("Vendas"),
      shiny::p("Conteúdo da segunda tela")
    )
  )
)

ui <- navbar(
  title = "Meu Dashboard",
  menu_items = menu_items
)
}
}
